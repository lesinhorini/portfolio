<h1>Job Items</h1>

<% @jobs_items.each do |job| %>
    <article>
        <h2><%= job.title %></h2>
        <h4><%= job.subtitle %></h4>
        <p><%= job.body %></p>
        <%= image_tag job.thumb_image if job.thumb_image? %>
    </article>
<% end %>
